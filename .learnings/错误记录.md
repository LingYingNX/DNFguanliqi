# DNF 补丁管理器错误记录

## [ERR-20260721-001] 启动器找不到 Electron 二进制

**Logged**: 2026-07-21T09:30:00+08:00  
**Priority**: critical  
**Status**: resolved  
**Area**: infra, config

### Summary
`启动DNF补丁管理器.bat` 无法打开界面，Windows 报找不到 `node_modules\\electron\\dist\\electron.exe`。

### Error

```text
Windows 找不到 ...\\node_modules\\electron\\dist\\electron.exe
```

### Context

仓库从嵌套目录搬迁到新根目录后，Electron npm 包目录存在，但 `dist/electron.exe` 尚未下载。旧启动器没有验证二进制，也没有触发 Electron 安装脚本。

### Suggested Fix

启动器在 `pnpm install --frozen-lockfile` 后执行 `pnpm exec electron --version`，再次检查真实 EXE；失败时返回明确错误。

### Metadata
- Reproducible: yes
- Related Files: `启动DNF补丁管理器.bat`
- Tags: launcher, electron, migration

### Resolution
- Resolved: 2026-07-21T09:45:00+08:00
- Commit: `f15bbc8`
- Notes: 临时移走 Electron dist 后，BAT 自动下载并通过 `--check`；真实启动窗口标题为 `DNF 补丁管理器`。

## [ERR-20260721-002] 新旧仓库目录替换中的 Windows 锁

**Logged**: 2026-07-21T09:30:00+08:00  
**Priority**: medium  
**Status**: resolved  
**Area**: infra

### Summary
旧 `重构` 目录被同步/系统组件持有，普通 PowerShell 删除失败。

### Error

```text
The process cannot access the file because it is being used by another process.
```

### Context

旧目录中残留旧版 EXE、运行数据和 NPK。进程列表没有显示旧程序，但目录句柄仍被占用。

### Suggested Fix

确认旧目录不再承载源码后，使用 Windows `MoveFileEx` 的 `MOVEFILE_DELAY_UNTIL_REBOOT` 登记删除；不要终止未确认的同步或系统进程。

### Metadata
- Reproducible: yes
- Related Files: `E:\codex目录\DNF`
- Tags: windows, cleanup, file-lock

### Resolution
- Resolved: 2026-07-21T09:20:00+08:00
- Notes: 旧版内容已清除，剩余目录已登记重启删除。

## [ERR-20260721-003] 工具编排字符串和 PowerShell 转义

**Logged**: 2026-07-21T09:30:00+08:00  
**Priority**: low  
**Status**: resolved  
**Area**: config, docs

### Summary
外层 JavaScript、PowerShell 和 BAT 多层解析会使 `$LASTEXITCODE`、反斜杠或中文路径被提前展开。

### Context

一次 BAT 回归实际成功，但外层脚本把 `$LASTEXITCODE` 提前展开为空，误报了自己的退出判断。类似问题也会让复杂并行命令输出丢失。

### Suggested Fix

复杂 PowerShell 使用独立调用或 encoded command；验证工具返回值时不要让外层 shell 解析 `$`；失败命令拆开重跑。

### Metadata
- Reproducible: yes
- Related Files: `docs/MAINTENANCE.md`
- Tags: powershell, tooling, diagnostics

### Resolution
- Resolved: 2026-07-21T09:50:00+08:00
- Notes: BAT 与 PowerShell 验证拆分，工具错误和应用错误分别记录。

## [ERR-20260721-004] lint 扫描运行时状态文件

**Logged**: 2026-07-21T09:55:00+08:00  
**Priority**: medium  
**Status**: resolved  
**Area**: config, tests

### Summary
Biome 把 `data/category-order.json` 当成源码格式化，导致 lint 失败。

### Context

`data/` 是应用运行时状态目录，不应因用户排序状态的 JSON 排版影响源码门禁。

### Suggested Fix

在 `biome.json` 排除 `data` 和 `patch-categories`，继续由真实应用和集成测试验证它们的内容。

### Metadata
- Reproducible: yes
- Related Files: `biome.json`, `data/category-order.json`
- Tags: biome, runtime-data, lint

### Resolution
- Resolved: 2026-07-21T09:56:00+08:00
- Commit: `db9381a`
- Notes: Biome 恢复 156 个文件通过。

## [ERR-20260721-005] 全量测试的时序超时

**Logged**: 2026-07-21T09:57:00+08:00  
**Priority**: low  
**Status**: resolved  
**Area**: tests

### Summary
全量 Vitest 第一次运行中 `view-controls` 测试超时，聚焦运行和第二次全量运行通过。

### Error

```text
tests/renderer/view-controls.test.tsx > switches to an aligned list while preserving selection and search
Test timed out in 10000ms
```

### Context

失败测试单独运行通过，第二次 `pnpm vitest run --maxWorkers=2` 以 44 个文件、182 项测试全部通过；没有发现业务代码回归。

### Suggested Fix

遇到单项超时先独立重跑并保留完整输出，再决定是否修改测试；不要把一次并行资源波动直接当成产品缺陷。

### Metadata
- Reproducible: unknown
- Related Files: `tests/renderer/view-controls.test.tsx`
- Tags: vitest, flaky, timing

### Resolution
- Resolved: 2026-07-21T09:52:46+08:00
- Notes: 聚焦测试和第二次全量门禁均通过，未修改业务逻辑。

## [ERR-20260721-006] PowerShell 不支持用 && 串联 Git 写操作

**Logged**: 2026-07-21T12:10:00+08:00  
**Priority**: low  
**Status**: resolved  
**Area**: config, docs

### Summary
在当前 Windows PowerShell 中使用 `&&` 串联 `git add` 和 `git commit` 会直接触发语法错误。

### Error

```text
The token '&&' is not a valid statement separator in this version.
```

### Context

尝试把文档暂存和提交写在同一个 `rtk git add ... && rtk git commit ...` 命令中；PowerShell 在执行 Git 前就拒绝了解析，仓库没有发生写入。

### Suggested Fix

Git 写操作必须分开调用：先单独执行 `rtk git add`，确认暂存状态，再单独执行 `rtk git commit`。不要在当前 PowerShell 环境照搬 Bash 连接符。

### Metadata
- Reproducible: yes
- Related Files: `docs/MAINTENANCE.md`, `E:\codex目录\DNF`
- Tags: powershell, git, tooling

### Resolution
- **Resolved**: 2026-07-21T12:10:00+08:00
- **Commit**: `a3df350`
- **Notes**: 改为分开的 Git 调用；原失败命令未修改工作树。

## [ERR-20260721-007] 启动窗口保存到断开副屏

**Logged**: 2026-07-21T12:35:00+08:00  
**Priority**: high  
**Status**: resolved  
**Area**: frontend, infra

### Summary
BAT 和 Electron 都启动成功，但主窗口位置保存在已断开的副屏坐标，用户看不到界面。

### Error

```text
Electron process exists, but the main window is outside the current display work area.
Observed bounds: 2376,604-3816,1504
Current primary work area: 0,0-1920,1040
```

### Context

`启动DNF补丁管理器.bat --check`、Electron 二进制和 `out/main/main.js` 均正常。真实进程窗口标题为 `DNF 补丁管理器`，但窗口坐标位于第二块显示器；应用只设置了 `show: false` 和 `ready-to-show`，没有检查保存的窗口坐标是否仍在当前显示器集合内。

### Suggested Fix

创建窗口后读取当前所有显示器工作区；若窗口矩形与所有工作区都不相交，则把它居中到主显示器，正常可见位置保持不变。

### Metadata
- Reproducible: yes
- Related Files: `src/main/main.ts`, `src/main/window-placement.ts`, `tests/unit/window-placement.test.ts`
- Tags: electron, launcher, window-placement, multi-monitor

### Resolution
- **Resolved**: 2026-07-21T12:40:00+08:00
- **Commit**: pending
- **Notes**: 新增窗口位置恢复逻辑和 3 个单元测试；`pnpm typecheck`、窗口定位测试、`pnpm build` 通过；通过 BAT 冷启动后窗口为可见、响应正常，矩形 `240,70-1680,970`。

## [ERR-20260804-001] 合并遗漏操作逻辑分支

**Logged**: 2026-08-04T13:06:24+08:00  
**Priority**: high  
**Status**: resolved  
**Area**: config, tests

### Summary
用户报告 worktree 分支合并到 master 后功能没有出现；根因是操作逻辑分支的提交从未真正进入 master。

### Error

```text
软件没有多出 worktrees 分支功能；master 检查显示只包含外观分支结果，缺少手动游戏目录输入、递归分类/补丁计数、分类移动、拖放和框选。
```

### Context

- 操作：将 `操作逻辑` 合并到 `master`
- 预期：`939c37f` 和 `b0b8203` 进入 master
- 实际：master 停留在 `e1d4a7a`，操作逻辑功能全部缺失
- 原因：之前的操作只把外观分支纳入 master，没有真正执行包含操作逻辑的合并；不是冲突覆盖，而是提交从未合并

### Suggested Fix

在独立 worktree 中执行真实 `git merge --no-ff`，逐项解决冲突，并在提交前验证 `master` 已包含操作逻辑 head。

### Metadata
- Reproducible: yes
- Related Files: `E:\codex目录\DNF`, `src/shared/ipc-contracts.ts`, `src/renderer/components/ItemWorkspace.tsx`, `tests/e2e`
- Pattern-Key: vcs.incomplete-merge

### Resolution
- **Resolved**: 2026-08-04T13:06:24+08:00
- **Commit/PR**: `f0ee8cd`
- **Notes**: 已生成真实合并提交 `f0ee8cd`，父提交为 `e1d4a7a` 和 `b0b8203`；相关 typecheck/lint/单元/集成/E2E 验证通过，已切换到 master 并清理已合并分支。

---

## [ERR-20260807-001] 逻辑组回收 E2E 仍按物理组恢复

**Logged**: 2026-08-07
**Priority**: medium
**Status**: resolved
**Area**: tests, recycle

### Summary

逻辑组回收后，回收站会为组内每个 NPK 展示独立的恢复项；旧 E2E 仍假设物理组只有一个恢复项，导致恢复按钮的严格定位失败。

### Error

```text
getByRole('button', { name: '恢复', exact: true }) resolved to 2 elements
```

### Context

虚拟组设计要求组成员保持普通 NPK 文件，回收逻辑组时回收每个成员并删除组元数据；恢复成员不会自动重建逻辑组。集成测试已覆盖该行为，但 `library-workflow.e2e.ts` 仍沿用旧物理目录组断言。

### Resolution

E2E 改为验证两个恢复项并逐个恢复，确认组卡片不重现、两个 NPK 内容恢复到原目录。`pnpm typecheck`、Biome、全量 Vitest、构建、启动器检查和完整 `library-workflow.e2e.ts` 均通过。

### Metadata

- Related Files: `tests/e2e/library-workflow.e2e.ts`, `src/core/recycle/recycle-items.ts`, `tests/integration/recycle-service.test.ts`
- Reproducible: yes
- Commit: pending
- Tags: virtual-group, recycle, e2e

---

## [ERR-20260811-001] 当前终端找不到 rtk 命令

**Logged**: 2026-08-11T15:30:00+08:00
**Priority**: medium
**Status**: pending
**Area**: infra, config

### Summary

仓库要求所有 shell 命令使用 `rtk` 前缀，但当前 Codex PowerShell 会话中 `rtk` 不在 PATH，无法执行。

### Error

```text
rtk : The term 'rtk' is not recognized as the name of a cmdlet, function, script file, or operable program.
```

### Context

- 任务：实现字体外观设置并运行验证。
- 现象：`rtk powershell ...`、`rtk rg ...` 均无法启动。
- 处理：改用 PowerShell 内建命令和 `pnpm` 完成同等读取、测试、构建和 lint 验证。

### Suggested Fix

检查 Codex 终端启动环境是否加载 RTK 安装目录，并在任务开始时运行 `Get-Command rtk`；若不可用，应提供明确的受支持回退命令或修复 PATH。

### Metadata

- Reproducible: yes
- Related Files: `@C:\Users\LYNX\.codex\RTK.md`
- Tags: rtk, powershell, tooling
- Pattern-Key: shell.command-not-found
- Recurrence-Count: 1

---
