# DNF插件管理器维护索引

> 更新日期：2026-07-07

## 入口说明

这是 `E:\BaiduSyncdisk\知识库\DNF插件管理器` 的日常维护入口。后续接手 DNF 补丁管理器时，先读本文件，再按任务类型读取对应文档。

当前项目主路径：

- 项目根目录：`E:\BaiduSyncdisk\codex目录\DNF`
- 应用源码目录：`E:\BaiduSyncdisk\codex目录\DNF\dnf-patch-manager`
- 知识库目录：`E:\BaiduSyncdisk\知识库\DNF插件管理器`
- 截图记录目录：`E:\BaiduSyncdisk\codex目录\DNF\codex截图记录`

## 文档分工

- `项目总览.md`：项目定位、技术栈、目录结构、核心数据模型和当前仓库状态。
- `功能请求.md`：已实现功能、明确不做事项、候选功能、用户已定案的交互边界。
- `学习记录.md`：长期维护经验、项目坑点、验证纪律和后续维护规则。
- `错误记录.md`：历史问题、根因、修复方式、回归验证方式。
- `归档/`：旧长文档、旧副本、阶段性整理材料和映射审计，只作为追溯证据，不作为日常入口。

## 推荐阅读顺序

1. 日常接手：先读 `维护索引.md`，再读 `项目总览.md`。
2. 改功能：补读 `功能请求.md`，确认哪些行为已经定案，哪些明确不做。
3. 修问题：补读 `错误记录.md`，避免重复踩坑。
4. 涉及打包、截图、真实文件验证、仓库整理：补读 `学习记录.md`。
5. 需要追溯来源：再查 `归档/`，不要把归档内容直接当成当前事实。

## 当前权威约定

- 项目已经初始化 Git，根目录是 `E:\BaiduSyncdisk\codex目录\DNF`。
- `dnf-patch-manager` 已从嵌套 Git 仓库转换为主仓库内普通源码目录。
- 组的定义已经固定：组就是一个真实文件夹，组内 NPK 文件整体启用 / 停用。
- 分类也以真实文件夹为准，不是虚拟分类树。
- 便携版数据必须放在 exe 同级的 `data` 和 `patch-categories`。
- 仓库远端当前只保留源码与构建必需文件；运行数据、截图、素材、样例补丁和维护资料只保留本地。
- 不做在线下载、社区分享、快照系统、复杂冲突检测、自动更新器和逆向保护。

## 后续维护要求

- 修改前必须读取当前源码和对应知识库文件，不能只凭历史记忆。
- 涉及 UI 的改动必须保留截图或自动化证据。
- 涉及文件移动、分组、启用、停用、回收站、排序时，必须验证真实文件路径结果。
- 每次会话结束要说明调用了什么、写入了什么；没有调用或写入也要明说。
- 后续新增功能请求写入 `功能请求.md`；新坑点写入 `学习记录.md` 或 `错误记录.md`。
- 同一仓库内的 Git 写操作必须串行执行，不要并行做 `add`、`commit`、`push`。

## 当前会话记录映射规则

- 本知识库后续维护必须优先参考当前会话记录，再更新主文件。
- 已审计会话记录：`C:\Users\LYNX\.codex\sessions\2026\06\28\rollout-2026-06-28T15-46-26-019f0d31-9500-7dc3-b34a-aad1a69baa2e.jsonl`
- 映射规则：
  - 功能和用户定案写入 `功能请求.md`
  - 项目路径、版本、仓库状态写入 `项目总览.md`
  - 经验和维护规则写入 `学习记录.md`
  - 失败、根因、修复和验证写入 `错误记录.md`
  - 过程证据写入 `归档/`

## 2026-07-04 知识库清理记录

- 根目录只保留日常维护主文件：`维护索引.md`、`项目总览.md`、`功能请求.md`、`学习记录.md`、`错误记录.md`
- 旧副本 `功能请求 1.md`、`学习记录 1.md`、`错误记录 1.md` 的有效内容已经合并回主文件
- 旧副本和阶段性整理方案已移入 `归档/旧知识点合并-20260704/`

## 2026-07-07 会话沉淀补充

- 本轮 Git / GitHub 维护结论：远端统一为 `master`，远端仓库为 `1735774501/DNFguanliqi.git`
- 本轮仓库边界结论：远端只保留源码与构建必需文件，运行数据、截图、素材、样例补丁和维护资料只保留本地
- 本轮工具踩坑：同仓库 Git 写操作不能并行，当前 PowerShell 环境不能直接使用 `&&`
