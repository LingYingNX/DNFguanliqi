# DNF 补丁管理器功能请求

更新时间：2026-08-03

## 本次请求

### [FEAT-20260803-001] 跨会话知识沉淀

状态：已完成本轮记录，持续维护

用户要求记录当前项目其它会话，长期维护、迭代、提炼并沉淀到 E:\BaiduSyncdisk\知识库\DNF补丁管理器。

本轮结果：

- 审阅了当前项目高信号 Codex transcript。
- 将完成项、部分完成项、未完成项、验证结果和当前仓库漂移分开记录。
- 按 self-improving-agent 写入源码仓库 .learnings。
- 知识库保持固定五文件根布局。

后续每次重要迭代都应追加到源码仓库 .learnings，再把稳定规则提炼到本目录。

## 历史请求与当前状态

### [FEAT-20260802-001] Windows 式卡片框选

状态：会话中已实现并验证

从主卡片管理面板空白、网格间隙或预览热点拖动，框内卡片被选中；卡片主体继续保留原生拖放。下一次修改事件链时要保持按钮、表单和菜单排除边界。

### [FEAT-20260803-002] 仅拖拽 NPK 导入

状态：会话中已实现并验证

移除空库中的文件选择入口，只允许拖拽 NPK；img、txt 等后缀拒绝。核心复制服务保留，跨进程旧入口同步清理。

### [FEAT-20260803-003] 补丁卡片源文件

状态：会话中已实现并验证

补丁卡片右键菜单加入源文件，调用受 schema 校验的 IPC，并用 shell.showItemInFolder 定位补丁库内对应 NPK。由于未持久化拖入前外部路径，源文件语义是库内文件。

### [FEAT-20260803-004] 分类树跨层级拖拽

状态：会话中已实现并验证

中部成为子级，上/下边缘成为同级前/后；真实移动目录、保留嵌套子分类、迁移顺序文件并刷新导航；拒绝非法目标。

### [FEAT-20260803-005] 分类树视觉反馈

状态：会话中已实现，当前 CSS 未提交

选中文件夹显示整行紫色边框，before/after 显示带留白的短横线，inside 保持整行高亮。当前工作区对应文件为 src/renderer/styles/layout.css，未在本次任务中处理。

## 待重新确认

- 预览同名图片在移动、回收、恢复和解散组后的生命周期。
- appearance worktree 的 54 文件范围和最终审查结论。
- 公开 Git 历史旧资料的远端清理状态。

## 明确边界

- 只管理用户已有的本地 NPK。
- 分类和组对应真实磁盘目录。
- 不做在线下载、社区分享、自动更新、逆向保护或游戏逻辑修改。
