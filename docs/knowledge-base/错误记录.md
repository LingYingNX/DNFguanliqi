# DNF 补丁管理器错误记录

更新时间：2026-08-03

## [ERR-20260718-001] 公开历史保留旧资料

**状态**：pending-reverify  
**来源**：019f72e4-c768-7472-9739-fb5c8e8ff054

安全审查发现，当前清理提交并不能移除公开 Git 历史中的旧截图、NPK、运行数据、绝对本地路径和旧资料；当时结论为 BLOCK/REQUEST_CHANGES。本次没有访问远端，不能把它标为已解决。

后续动作：确认远端可见性和历史清理策略，再重新生成审查证据。

## [ERR-20260719-001] Electron E2E 并行超时

**状态**：isolated-pass  
**来源**：019f78b1-3a13-7911-8b68-157c621d314e

完整 Electron 批次在 124 秒内没有完成，不能算通过；单 worker 的 game-directory 场景随后 1 passed、18.8s。记录完整失败、隔离命令和最终结果，不删除失败上下文。

## [ERR-20260727-001] Vitest 全量一次性超时

**状态**：isolated-pass  
**来源**：019fa2a8-5146-7b00-ae91-856a9fe1d14c

第一次全量为 46/48 文件、208/210 测试；两个失败未在单独运行、串行运行和第二次全量中复现，随后恢复为 48/48、210/210。结论是当时的并行资源/时序抖动，未据此修改生产逻辑。

## [ERR-20260731-001] 预览生命周期测试缺口

**状态**：pending  
**来源**：019fb760-0e83-7692-8226-a3b49b9a8028

只读审查发现预览路径已转向库内同名图片，但移动、回收和解散组路径可能只处理 NPK 或 marker；既有测试主要覆盖旧 managed preview。必须按当前源码补真实文件生命周期验证。

## [ERR-20260802-001] 框选命中区域过窄

**状态**：resolved-in-session  
**来源**：019fc14e-9476-7ff1-9d81-ceea2ed69ca3

旧框选只监听内部 item-grid，并要求事件目标等于当前目标，导致从主面板内边距、网格间隙和预览热点开始拖动时失败。会话中扩大到主面板并保留卡片主体拖放，相关单测和真实 Electron 回归通过。

## [ERR-20260802-002] 分类数量没有遵循包含子分类开关

**状态**：resolved-in-session  
**来源**：019fc14e-9476-7ff1-9d81-ceea2ed69ca3

分类树 patchCount 递归累计子孙文件，导致未勾选包含子分类时左侧数量仍包含子目录。会话修正直接数量、当前工作区数量和全部视图递归数量，并以集成与 Electron 回归验证。

## [ERR-20260803-001] 分类顺序文件异步写入竞态

**状态**：resolved-in-session  
**来源**：019fc14e-9476-7ff1-9d81-ceea2ed69ca3

跨父级移动 E2E 在目录移动完成后立即读取 category-order.json，原子重命名尚未完成，造成假失败。诊断确认生产写入正确，测试改为等待最终文件出现后再解析，并移除临时诊断输出。

## [ERR-20260803-002] 本次记录过程中的工具错误

**状态**：resolved  
**来源**：本次会话

- session-events.py 在 GBK stdout 下遇到 UnicodeEncodeError；改用 python -X utf8。
- Codex app 的 list_threads 上限为 50；改用 limit=50 和本地候选器。
- 嵌套 PowerShell 展开了内层 $doc、$_.Name；改用转义变量和隔离调用。
- 首个 apply_patch 尾标记解析失败；后续使用精确的 *** End Patch 结尾。
- provenance helper 未识别 .agents 安装面；source/version 保持 unknown，不把它当作项目代码错误。
- 关闭 YAML 条目时，过宽的状态 patch 临时误改了旧 provenance 条目和预览生命周期缺口；已恢复原状态并改用唯一条目标题。
- 两次按行诊断命令因外层 PowerShell 展开 `$lines`、`$i` 而失败；改用 `Get-Content -Tail` 完成检查。
- 最终状态断言脚本的动态正则未转义条目 ID 中的连字符；改用字符串分段校验。
- 最终哈希命令因外层 PowerShell 展开 `$_` 而失败；改用 Python 文件 API 和 SHA-256 校验。

## [ERR-20260803-003] YAML 验证命令的嵌套 PowerShell 引号错误

**状态**：resolved  
**来源**：本次会话

验证 `mempalace.yaml` 的第一次命令在进入 YAML 解析前就因外层 PowerShell 解释了内层引号/变量而失败；该失败不能作为 YAML 内容无效的证据。后续改用单层 Python YAML 解析器验证，文件可解析且 `rooms.general` 结构符合预期。

关联：项目 `.learnings/ERRORS.md` 中的 `ERR-20260803-006`。

## [ERR-20260803-004] 错误日志 patch 锚点不匹配

**状态**：resolved  
**来源**：本次会话

第一次追加日志时使用了实际文件中不存在的 `Notes` 文本锚点，`apply_patch` 拒绝应用，未写入任何内容。重新读取文件尾部后按实际内容重建 patch。
